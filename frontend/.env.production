# Build hébergé — URLs relatives (VITE_API_URL vide = /api/... relatif)
# Nginx route /api/ vers le backend de chaque client
# NE PAS mettre de clé Anthropic ici — elle reste côté backend uniquement
VITE_API_URL=
VITE_API_TOKEN=
VITE_APP_MODE=production
